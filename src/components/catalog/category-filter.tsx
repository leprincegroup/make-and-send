"use client";

// Category filter is no longer used — the product is fully custom (no catalog).
// This file is kept as a placeholder to avoid import errors from any remaining references.

export function CategoryFilter() {
  return null;
}
